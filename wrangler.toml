name = "blog"
compatibility_date = "2026-04-22"
compatibility_flags = ["nodejs_compat"]

# Astro + @astrojs/cloudflare generates the worker entry
# No need for [site] - the adapter handles everything
